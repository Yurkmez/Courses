<div class='navbar-fixed'>
    <nav>
        <div class='nav-wrapper'>
            {{#if isAuth}}
                <a class='sessionName' href='/' class='brand-logo'>
                    Session:
                    {{adminName.name}}</a>
            {{/if}}
            <ul id='nav-mobile' class='right hide-on-med-and-down navFont'>
                {{#if isHome}}
                    <li class='active'>
                        <a href='/'>Home</a></li>
                {{else}}
                    <li> <a href='/'>Home</a></li>
                {{/if}}

                {{#if isAuth}}

                    {{#if isCourses}}
                        <li class='active'> <a href='/courses'>Courses</a></li>
                    {{else}}
                        <li> <a href='/courses'>Courses</a></li>
                    {{/if}}
                    {{#if isAdd}}
                        <li class='active afterAdd'><a href='/course_add'>Add
                                courses</a></li>
                    {{else}}
                        <li><a class='afterAdd' href='/course_add'>Add courses</a></li>
                    {{/if}}
                    {{#if isBasket}}
                        <li class='active colorCustomer'><a
                                href='/basket/{{adminId._id}}'
                            >Basket</a></li>
                    {{else}}
                        <li><a
                                class='colorCustomer'
                                href='/basket/{{adminId._id}}'
                            >Basket</a></li>
                    {{/if}}
                    {{#if isOrder}}
                        <li class='active colorCustomer'><a
                                href='/orders/{{adminId._id}}'
                            >Orders</a></li>
                    {{else}}
                        <li><a
                                class='colorCustomer'
                                href='/orders/{{adminId._id}}'
                            >Orders</a></li>
                    {{/if}}
                    <li><a class='sign_out' href='authadmin/logout'>Sign out</a></li>
                    {{#if isProfile}}
                        <li class='active'><a
                                class='colorProfile'
                                href='/profile'
                            >Profile</a></li>
                    {{else}}
                        <li><a
                                class='colorProfile'
                                href='/profile'
                            >Profile</a></li>
                    {{/if}}
                {{else}}
                    {{#if isLogin}}
                        <li class='active'><a href='/authadmin/login'>Sign in</a></li>
                    {{else}}
                        <li><a href='/authadmin/login'>Sign in</a></li>
                    {{/if}}
                {{/if}}
            </ul>
        </div>
    </nav>
</div>